# Git 처음 사용자용: GitHub에 hrdi2026-06-06 만들고 소스코드 올리기

이 문서는 **처음 하는 사람**도 그대로 따라할 수 있게 만들었습니다.

목표는 2가지입니다.
1. GitHub에 `hrdi2026-06-06` 저장소 만들기
2. 내 컴퓨터의 소스코드를 GitHub에 올리기(push)

---

## 0) 준비물

아래 3가지만 있으면 됩니다.
- Git 설치 완료
- GitHub 계정
- 소스코드 폴더(예: 내 프로젝트 폴더)

---

## 1) GitHub에서 빈 저장소 만들기

1. GitHub 로그인
2. 오른쪽 위 `+` 버튼 클릭 -> `New repository`
3. Repository name에 `hrdi2026-06-06` 입력
4. Public 또는 Private 선택
5. `Create repository` 클릭

중요: **README 자동 생성은 체크하지 않는 것**이 초보자에게 충돌이 적습니다.

---

## 2) 프로젝트 폴더로 이동하기

PowerShell(또는 터미널)에서 아래처럼 입력합니다.

```powershell
cd "내_프로젝트_폴더_경로"
```

예시:

```powershell
cd "C:\Users\HPE\Downloads\2-Day-RAG-Based-AI-Application-Design-2026-main\2-Day-RAG-Based-AI-Application-Design-2026-main"
```

---

## 3) Git 저장소 시작하기

```powershell
git init
git checkout -B main
```

설명:
- `git init`: 이 폴더를 Git으로 관리 시작
- `git checkout -B main`: 브랜치를 main으로 맞춤

---

## 4) 처음 1번만 하는 사용자 정보 설정

아직 설정 안 했으면 아래를 한 번만 실행합니다.

```powershell
git config --global user.name "내GitHub아이디"
git config --global user.email "내이메일@example.com"
```

---

## 5) 파일 저장(커밋)하기

```powershell
git add -A
git commit -m "Initial commit"
```

설명:
- `git add -A`: 모든 파일을 업로드 준비
- `git commit -m "..."`: 저장 스냅샷 만들기

---

## 6) GitHub 저장소와 연결하기

아래에서 `내아이디`는 본인 GitHub 아이디로 바꿔 입력하세요.

```powershell
git remote add origin https://github.com/내아이디/hrdi2026-06-06.git
```

이미 origin이 있다고 나오면:

```powershell
git remote remove origin
git remote add origin https://github.com/내아이디/hrdi2026-06-06.git
```

---

## 7) GitHub로 올리기(push)

```powershell
git push -u origin main
```

성공하면 다음부터는 짧게 이렇게만 입력해도 됩니다.

```powershell
git add -A
git commit -m "수정 내용"
git push
```

---

## 8) 자주 나오는 문제 해결

### Q1. `fatal: not a git repository`
현재 폴더가 프로젝트 폴더가 아닙니다.

해결:
```powershell
cd "내_프로젝트_폴더_경로"
git init
```

### Q2. `remote origin already exists`
이미 원격 주소가 등록된 상태입니다.

해결:
```powershell
git remote remove origin
git remote add origin https://github.com/내아이디/hrdi2026-06-06.git
```

### Q3. 로그인/인증 오류
GitHub 인증이 필요합니다.

해결(권장): GitHub CLI 사용 시
```powershell
gh auth login
```

---

## 9) 한 번에 보는 최소 명령어 모음

```powershell
cd "내_프로젝트_폴더_경로"
git init
git checkout -B main
git add -A
git commit -m "Initial commit"
git remote add origin https://github.com/내아이디/hrdi2026-06-06.git
git push -u origin main
```

여기까지 끝나면 GitHub 저장소 페이지에서 파일이 올라왔는지 확인하면 됩니다.